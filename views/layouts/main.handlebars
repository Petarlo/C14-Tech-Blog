<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <script src="https://kit.fontawesome.com/965a925bc3.js" crossorigin="anonymous"></script>
</head>

<body class="bg"> 
          <header class="navbar bg-dark border-bottom border-body x-5" data-bs-theme="dark"> 
          <h1>
            <i class="fa-solid fa-code" style="color: #ccc;"></i>
            <a class="badge text-bg-dark fs-2 p-0" style="color: #ccc;" href="/">The Tech Blog</a>
          </h1>
            <!-- Conditionally render login or logout links -->
            <nav class="nav justify-content-end"></div>
            {{#if logged_in}}
            <a class="nav-link text-light fw-bold fs-3 mx-2" href="/">Home</a>
            <a class="nav-link text-light fw-bold fs-3 mx-2" href="/dashboard">Dashboard</a> 
            <button class="no-button nav-link text-light fw-bold fs-3 mx-2" id="logout">Logout</button>
            {{else}}
            <a class="nav-link text-light fw-bold fs-3 mx-2" href="/login">Login</a>
            {{/if}}
          </nav>
        </header>
        
        <main class="container container-fluid mt-5">
          <!-- Render the sub layout -->
          {{{ body }}}
        </main>

        <footer>
        </footer>
    
      <!-- Render script for logged in users only -->
      {{#if logged_in}}
      <script src="/js/logout.js"></script>
      {{/if}}

</body>
</html>